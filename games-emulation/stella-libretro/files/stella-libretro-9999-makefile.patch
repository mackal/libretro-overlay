diff --git a/Makefile b/Makefile
index 230490d..347a426 100644
--- a/Makefile
+++ b/Makefile
@@ -104,9 +104,9 @@ OBJECTS := $(SOURCES:.cpp=.o) $(SOURCES_C:.c=.o)
 all: $(TARGET)
 
 ifeq ($(DEBUG),0)
-   FLAGS += -O3 -ffast-math -funroll-loops 
+   FLAGS += -ffast-math -funroll-loops 
 else
-   FLAGS += -O0 -g
+   FLAGS += -g
 endif
 
 LDFLAGS += $(fpic) -lz $(SHARED)
